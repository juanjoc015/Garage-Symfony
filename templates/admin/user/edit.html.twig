{% extends 'admin/layouts/base.html.twig' %}

{% block title %}Edition de données de l'utilisateur #
	{{ user.id }}
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Edition de données de l'utilisateur</h1>

		{{ include('admin/user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

		<a href="{{ path('admin_user_index') }}" class="btn btn-secondary">Retour</a>

		{{ include('admin/user/_delete_form.html.twig') }}
	</div>
{% endblock %}
