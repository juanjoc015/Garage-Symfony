<!DOCTYPE html>

<title>Edit Hour</title>

{% block body %}
    <h1>Edit Hour</h1>

    {{ include('admin/hour/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_hour_index') }}">back to list</a>

    {{ include('admin/hour/_delete_form.html.twig') }}
{% endblock %}
