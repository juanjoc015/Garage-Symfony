<!DOCTYPE html>

<title>New Hour</title>

{% block body %}
    <h1>Create new Hour</h1>

    {{ include('admin/hour/_form.html.twig') }}

    <a href="{{ path('admin_hour_index') }}">back to list</a>
{% endblock %}
