{% extends 'admin/layouts/base.html.twig' %}

{% block title %}Voir la demande #{{ contact.id }} de contact{% endblock %}

{% block body %}

    <h1 class="m-3">Informations contact</h1>
    <table class="table m-3">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ contact.lastname }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ contact.firstname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ contact.phone }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_contact_index') }}" class="btn btn-secondary m-3">Retour</a>

    {{ include('admin/contact/_delete_form.html.twig') }}
{% endblock %}
