{% extends 'admin/layouts/base.html.twig' %}

{% block title %}Voir la demande #{{ contact.id }} de contact{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ contact.lastname }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ contact.firstname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ contact.phone }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_contact_index') }}" class="btn btn-secondary">Retour</a>

    {{ include('admin/contact/_delete_form.html.twig') }}
{% endblock %}
