{% extends 'admin/layouts/base.html.twig' %}

{% block title %}Editer le service #{{ service.id }}
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Editer le service #{{ service.id }}</h1>

		{{ include('admin/service/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

		<a href="{{ path('admin_service_index') }}" class="btn btn-secondary">Retour</a>

		{{ include('admin/service/_delete_form.html.twig') }}
	</div>
{% endblock %}
