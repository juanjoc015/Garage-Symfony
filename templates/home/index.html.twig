{% extends 'layouts/base.html.twig' %}

{% block body %}
	{% include "home/partials/_service.html.twig" %}
	{% include "home/partials/_hours.html.twig" %}
	{% include "home/partials/_cars.html.twig" %}

	<section class="container my-5">
		<h3 class="text-center mb-4">Votre avis nous intéresse</h3>
		<div class="row">
			<div class="col-lg-8 mx-auto">
				{{ form_start(review_form) }}
				{{ form_widget(review_form) }}
				{{ form_end(review_form) }}

				{% for review in reviews %}{% endfor %}
			</div>
		</div>
	</section>

{% endblock %}
